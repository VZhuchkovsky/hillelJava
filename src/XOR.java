/**
 * Created by Влад on 14.04.2015.
 */
public class XOR {
    public static void main(String[] params) {
/*
        int a = 5;
        int b = 5;
        String message = new String();
        System.out.println();
*/
    }

    public static String crypt(String message, char[] key) {







        /*
        char[] code = message.toCharArray();
        for (int i = 0; i < code.length; i++) {
            code[i] = (char) (code[i] ^ key[i % key.length]);
            System.out.println("Исходная строка" + message);
            char[] chars = message.toCharArray();
            String someString = String.copyValueOf(chars);

*/
            //char crypedChar ^= key;
            return message;

        }


        //message = new String(chars);
        //return message;

        /*
        String txt = message.getBytes();
        String rkey = key.getBytes();
        String res = new byte[message.length()];

        for (int i = 0; i < txt.length; i++) {
            res[i] = (byte) (txt[i] ^ key[i % key.length]);
        }

        return res;

        char[] chars = message.toCharArray();
        for (int i = 0, a = 0; i < chars.length; i++, a++) {
            chars[i] = (char) (chars[i] ^ key[a]);
            if (a == key.length - 1) {
                a = 0;
*/

        //return message;
    }





